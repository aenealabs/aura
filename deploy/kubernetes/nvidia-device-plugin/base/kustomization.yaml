apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: nvidia-device-plugin

resources:
  - daemonset.yaml

commonLabels:
  app.kubernetes.io/part-of: aura-platform
  app.kubernetes.io/managed-by: kustomize
