# AWS Load Balancer Controller Helm Values (Qa Environment)
# AUTO-GENERATED by deploy/scripts/generate-k8s-config.sh
# Generated: 2026-01-15T22:00:58Z
#
# Usage:
#   helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller \
#     -n kube-system -f deploy/kubernetes/alb-controller/overlays/qa/values.yaml

clusterName: aura-cluster-qa
region: us-east-1
vpcId: vpc-0fedcba9876543210

serviceAccount:
  create: true
  name: aws-load-balancer-controller
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::234567890123:role/aura-alb-controller-role-qa

replicaCount: 2

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

enableShield: true
enableWaf: true
enableWafv2: true

logLevel: info

podDisruptionBudget:
  maxUnavailable: 1

affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - aws-load-balancer-controller
          topologyKey: kubernetes.io/hostname

nodeSelector:
  kubernetes.io/os: linux
