# CLAUDE.md

This file provides guidance to Claude Code when working with code in the **Project Aura** repository.

**Project Aura** is an autonomous AI SaaS platform enabling machines to reason across entire enterprise codebases through a hybrid graph-based architecture. It uses adaptive intelligence to automate security patching in sandbox environments with human-in-the-loop approval processes.

---

## Project Overview

### Core Capabilities

- **Autonomous Code Intelligence:** Multi-agent system orchestrates vulnerability detection, patch generation, and remediation
- **Hybrid GraphRAG:** Combines Neptune graph database + OpenSearch vector search for comprehensive code understanding
- **Security-First:** NIST 800-53 aligned controls, SOX compliance requirements
- **HITL Workflow:** Human-in-the-loop approval for critical patches and deployments
- **Sandbox Testing:** Isolated network environments for safe patch validation

### Architecture

- **Agent Orchestrator:** Coordinates Coder, Reviewer, and Validator agents
- **Context Retrieval Service:** Hybrid GraphRAG query engine (graph structural + vector semantic)
- **Sandbox Network Service:** Provisions ephemeral isolated environments
- **Monitoring Service:** Tracks agent health, performance, security metrics

---

## Documentation Standards

### CRITICAL: AI Attribution Policy

**NEVER include AI attribution in any project documentation or commit messages:**

- ❌ **Prohibited:** `Co-Authored-By: Claude <noreply@anthropic.com>`
- ❌ **Prohibited:** `🤖 Generated with [Claude Code](https://claude.com/claude-code)`
- ❌ **Prohibited:** Any references to Claude, AI assistance, or automated generation

**Reason:** All documentation must appear authored by human engineers to avoid confusion. Users attempting to email Claude for support is not a valid support channel. Only human engineers should be contacted for support questions.

**This rule applies to:**

- All commit messages (no co-author attributions)
- All documentation files (.md files)
- All code comments
- All README files
- All configuration files

**Exception:** Archived documentation (`archive/` directory) is obsolete and exempt from this rule.

**Enforcement:** A pre-commit hook automatically strips AI attribution from commit messages:

```bash
# Install the hook (required once per clone)
pre-commit install --hook-type commit-msg

# The hook runs automatically on every commit
# Manual test:
python -m scripts.security_hooks.commit_msg_hook <commit-msg-file>
```

See `scripts/security_hooks/commit_msg_hook.py` for implementation details.

---

## Documentation & Context References

### Core Documentation (Reference by path, read on-demand only)

- **Project Status:** `docs/PROJECT_STATUS.md` - Overall completion metrics, component status
- **Changelog:** `CHANGELOG.md` - Auto-generated by Release Please (see `docs/deployment/RELEASE_PLEASE_GUIDE.md`)
- **Design Principles:** `agent-config/design-workflows/design-principles.md` - Enterprise UI/UX design system
- **Design Review Workflow:** `agent-config/design-workflows/design-review-workflow.md` - 7-phase automated design review
- **App UI Blueprint:** `agent-config/design-workflows/app-ui-blueprint.md` - User personas, workflows, screen inventory, implementation roadmap
- **Research Findings:** `docs/research/RESEARCH_FINDINGS_CI_CD_DESIGN_WORKFLOWS.md` - CI/CD automation patterns

### Product Documentation (docs/product/)

- **Getting Started:**
  - `docs/product/getting-started/index.md` - Platform overview, key benefits, use cases
  - `docs/product/getting-started/quick-start.md` - 5-minute setup guide
  - `docs/product/getting-started/system-requirements.md` - Prerequisites for all deployment options
  - `docs/product/getting-started/installation.md` - SaaS, Kubernetes, Podman deployment
  - `docs/product/getting-started/first-project.md` - Repository onboarding walkthrough
- **Core Concepts:**
  - `docs/product/core-concepts/index.md` - Technology pillars overview
  - `docs/product/core-concepts/autonomous-code-intelligence.md` - LLM-powered remediation
  - `docs/product/core-concepts/hybrid-graphrag.md` - Neptune + OpenSearch architecture
  - `docs/product/core-concepts/multi-agent-system.md` - Agent orchestration
  - `docs/product/core-concepts/hitl-workflows.md` - 4 autonomy levels, 7 policy presets
  - `docs/product/core-concepts/sandbox-security.md` - ECS Fargate isolation model
- **User Guides:**
  - `docs/product/user-guides/dashboard-customization.md` - Dashboard widgets, layout editor, sharing (ADR-064)
  - `docs/product/user-guides/vulnerability-remediation.md` - Security workflow guide
  - `docs/product/user-guides/patch-approval.md` - HITL approval process
  - `docs/product/user-guides/team-collaboration.md` - Sharing and permissions

### Support Documentation (docs/support/)

- **Troubleshooting:**
  - `docs/support/troubleshooting/index.md` - Troubleshooting guide overview
  - `docs/support/troubleshooting/common-issues.md` - Frequent problems with error codes
  - `docs/support/troubleshooting/deployment-issues.md` - CloudFormation, EKS, containers
  - `docs/support/troubleshooting/performance-issues.md` - Latency, memory, scaling
  - `docs/support/troubleshooting/security-issues.md` - Auth, permissions, encryption
- **API Reference:**
  - `docs/support/api-reference/index.md` - API documentation overview
  - `docs/support/api-reference/rest-api.md` - REST endpoints, request/response
  - `docs/support/api-reference/graphql-api.md` - GraphQL schema and queries
  - `docs/support/api-reference/webhooks.md` - Webhook events, payloads, retry logic
- **Architecture:**
  - `docs/support/architecture/index.md` - Architecture documentation overview
  - `docs/support/architecture/system-overview.md` - High-level architecture diagrams
  - `docs/support/architecture/data-flow.md` - Data movement through system
  - `docs/support/architecture/security-architecture.md` - Controls, encryption, isolation
  - `docs/support/architecture/disaster-recovery.md` - Backup, recovery, RTO/RPO
- **Operations:**
  - `docs/support/operations/index.md` - Operations guide overview
  - `docs/support/operations/monitoring.md` - CloudWatch, dashboards, alerts
  - `docs/support/operations/logging.md` - Log formats, retention, analysis
  - `docs/support/operations/backup-restore.md` - Backup and restoration procedures
  - `docs/support/operations/scaling.md` - HPA, autoscaling, capacity planning
- **FAQ:** `docs/support/faq.md` - Frequently asked questions (11 categories)

### Specialized Agents (Reference by path)

- **Security Code Reviewer:** `agent-config/agents/security-code-reviewer.md` - OWASP Top 10, AI-specific threats, compliance, **Adaptive Security Intelligence Workflow**
- **Code Quality Reviewer:** `agent-config/agents/code-quality-reviewer.md` - Clean code, SOLID principles, technical debt
- **Performance Reviewer:** `agent-config/agents/performance-reviewer.md` - Bottleneck detection, scalability, optimization
- **Test Coverage Reviewer:** `agent-config/agents/test-coverage-reviewer.md` - Coverage gaps, test quality, anti-patterns
- **Documentation Accuracy Reviewer:** `agent-config/agents/documentation-accuracy-reviewer.md` - Doc-code sync, consistency

### Architecture & Workflow Documentation

- **HITL Sandbox Architecture:** `docs/design/HITL_SANDBOX_ARCHITECTURE.md` - Patch approval workflow, sandbox provisioning
- **System Architecture:** `SYSTEM_ARCHITECTURE.md` - High-level architecture, deployment topology
- **Adaptive Security Intelligence:** `agent-config/agents/security-code-reviewer.md#adaptive-security-intelligence-workflow` - Proactive threat monitoring → assessment → sandbox → HITL approval
- **Architecture Decision Records:** `docs/architecture-decisions/` - Documented rationale for significant design choices

### Network Services Documentation

- **dnsmasq Technical Guide:** `docs/integrations/DNSMASQ_INTEGRATION.md` - Comprehensive 3-tier DNS architecture
- **dnsmasq Quick Start:** `docs/integrations/DNSMASQ_QUICK_START.md` - 5-minute deployment guide

### Assessments & Audits

- **Time/Space Complexity Audit:** `docs/assessments/TIME_SPACE_COMPLEXITY_AUDIT.md` - Full codebase audit (661 files, 87 findings: 12 critical, 41 moderate, 34 low). Tracking issues: #701 (P0), #702 (P1), #703 (P2), #704 (P3)
- **AWS Well-Architected CI/CD:** `docs/assessments/AWS_WELL_ARCHITECTED_CICD_ASSESSMENT.md`
- **AWS Well-Architected Platform:** `docs/assessments/AWS_WELL_ARCHITECTED_PLATFORM_ASSESSMENT.md`
- **Dev Environment Cost Analysis:** `docs/assessments/COST_ANALYSIS_DEV_ENVIRONMENT.md`

### Research & Innovation

- **Research Directory:** `docs/research/` - Academic paper analysis, architecture proposals, experiments
- **Neural Memory Research:** `docs/research/papers/neural-memory-2025/` - Titans & MIRAS analysis (test-time training, deep MLP memory)
- **Architecture Proposals:** `docs/research/proposals/` - Pre-ADR proposals for major enhancements
- **Aerospace Agent Proposal (PROP-2026-004):** `docs/research/proposals/aerospace-defense-systems-engineer-agent.md` - Aerospace & defense systems engineer agent (MBSE, DO-178C, FAA certification, defense standards)
- **ADR-024 (Deployed):** `docs/architecture-decisions/ADR-024-titan-neural-memory.md` - Titan Neural Memory Architecture (237 tests, 5 phases complete)
- **ADR-051 (Deployed):** `docs/architecture-decisions/ADR-051-recursive-context-and-embedding-prediction.md` - RLM + VL-JEPA (MIT CSAIL + Meta FAIR Dec 2025 research)
- **RLM Package:** `src/services/rlm/` - RecursiveContextEngine (100x context scaling), REPLSecurityGuard (code validation), InputSanitizer (prompt injection prevention), 119 tests
- **JEPA Package:** `src/services/jepa/` - EmbeddingPredictor (2.85x inference efficiency), SelectiveDecodingService (fast path routing), 48 tests

---

## Quick Essentials (Load These Into Context)

### Design System (For UI/UX Work)

- **Primary Brand Color:** `#3B82F6` (blue)
- **Semantic Colors:**
  - Critical/Error: `#DC2626` (red)
  - High Priority: `#EA580C` (orange)
  - Medium Priority: `#F59E0B` (amber)
  - Success: `#10B981` (green)
  - Info: `#3B82F6` (blue)
- **Typography:** Inter font family, H1 32px, H2 24px, Body 14px, Code 13px (JetBrains Mono)
- **Spacing:** 8px base unit (4px, 8px, 12px, 16px, 24px, 32px, 48px)
- **Accessibility:** WCAG 2.1 AA compliance required (4.5:1 contrast minimum)
- **CSS Framework:** Tailwind CSS (utility-first, LLM-friendly)

### Security Requirements

- **Compliance:** NIST 800-53 aligned, SOX (FedRAMP/CMMC certifications not yet pursued)
- **Accessibility:** WCAG 2.1 AA mandatory
- **AI-Specific Threats:**
  - Prompt injection in LLM inputs
  - Agent confusion and tool use abuse
  - GraphRAG context poisoning
  - Sandbox escape (container breakout, network isolation bypass)
- **Authentication:** Argon2id password hashing, JWT with proper validation
- **Authorization:** RBAC with principle of least privilege
- **Container Base Images:** Private ECR only (see Container Security below)

### Container Security (MANDATORY)

**CRITICAL: All container builds MUST use private ECR base images from `aura-base-images`.**

- ❌ **NEVER use** public images (Docker Hub, public ECR, GitHub Container Registry)
- ❌ **NEVER use** `public.ecr.aws/*` images in production builds
- ✅ **ALWAYS use** private ECR base images: `{ACCOUNT_ID}.dkr.ecr.{REGION}.amazonaws.com/aura-base-images/{image}:{tag}`

**Available Private Base Images:**
```
aura-base-images/python:3.11-slim
aura-base-images/alpine:3.19
aura-base-images/node:20-slim
aura-base-images/nginx:1.25
```

**WORKFLOW REQUIREMENT - Before Building ANY Container Image:**

1. Set `AWS_ACCOUNT_ID` for the target environment
2. Construct the private base image URI using the ECR pattern above
3. Build with `--build-arg` to override the base image
4. **NEVER run a build without `--build-arg`** - public defaults waste time and tokens on rebuild

See `docs/deployment/DEPLOYMENT_GUIDE.md` for full container build workflow.

**Rationale:**
1. **Supply chain security:** Private images are scanned, hardened, and controlled
2. **Rate limit avoidance:** Docker Hub has pull rate limits that break CI/CD
3. **Compliance:** Controlled software supply chain (security best practice)
4. **Audit trail:** All base images tracked in private ECR with vulnerability scanning

**Dockerfiles must support `--build-arg` for base image override.** Default values in Dockerfiles are for local development only; CI/CD and production MUST override with private ECR images.

### Key Technology Stack

- **Backend:** Python 3.11+, FastAPI
- **LLM Integration:** OpenAI GPT-4, Anthropic Claude (via Bedrock)
- **Graph Database:** AWS Neptune (Gremlin)
- **Vector Database:** AWS OpenSearch
- **Container Orchestration:** AWS EKS (Kubernetes) with EC2 Managed Node Groups
- **Infrastructure:** CloudFormation, Terraform (planned)
- **Compute:** EKS on EC2 (GovCloud compatible), ECS on Fargate (for dnsmasq VPC-wide service)
- **DNS/Service Discovery:** dnsmasq (3-tier architecture)
- **Frontend (Future):** React 18+, TypeScript, Tailwind CSS, Next.js 14+
- **Deployment Strategy:** AWS Commercial Cloud (dev/qa) → AWS GovCloud (US) (production)

### Service Discovery Endpoints

- **Neptune:** `neptune.aura.local:8182`
- **OpenSearch:** `opensearch.aura.local:9200`
- **Context Retrieval Service:** `context-retrieval.aura.local:8080`
- **Agent Orchestrator:** `orchestrator.aura.local:8080`

### Current Status (As of 2026-03-04)

- **Company:** Aenea Labs | **Domain:** aenealabs.com (Route 53)
- **Overall Completion:** 99%
- **Total Lines of Code:** 439,000+ lines (193K Python, 142K Tests, 53K JS/JSX, 68K Config/Infrastructure)
- **Test Suite:** 23,165+ tests (16,499 passed, 6,666 skipped, 0 failed)
- **Architecture Decision Records:** 84 ADRs (83 Deployed/Accepted, 1 Proposed)
- **Infrastructure:** 100% (All 9 phases deployed: Foundation, Data, Compute, Application, Observability, Serverless, Sandbox, Security, Scanning Engine)
- **Modular CI/CD:** 100% (32 buildspecs managing all 155 CloudFormation templates)
- **CloudFormation Templates:** 155 templates (24 CodeBuild + 131 infrastructure)
- **CodeBuild Projects:** 19 projects (9 parent layers + 10 sub-layers including SSR, env-validator, and runtime-security)
- **Network Services:** 100% (dnsmasq DNSSEC deployed to EKS, private ECR image, ECS Fargate VPC-wide DNS)
- **LLM Integration:** 100% (Bedrock API operational, Claude 3.5 Sonnet approved and tested)
- **GovCloud Readiness:** 100% (19/19 deployed services compatible, using provisioned Neptune)
- **Autonomy Framework:** 100% (Configurable HITL, 7 policy presets, ADR-032)
- **Security Services:** 100% (5 services, 328 tests, 7 CloudWatch alarms, EventBridge bus, SNS alerts)
- **Self-Service Test Environments:** 100% (ADR-039 complete, 4 phases deployed, monitoring + budgets operational)
- **Context Engineering (ADR-034):** 100% (7 services deployed: scoring, registry, stack, retrieval, hoprag, mcp, summarization)
- **Hybrid GraphRAG (Issue #151):** 100% (Full graph search implementation: CALL_GRAPH, DEPENDENCIES, INHERITANCE, REFERENCES, RELATED)
- **AWS Agent Parity (ADR-037):** 100% (27 services total: AgentCore, Security, DevOps, Transform, Phase 2)
- **Agent Optimization (ADR-029):** Phases 1.3, 2.2, 2.3 ENABLED BY DEFAULT (semantic caching, self-reflection, A2AS security)
- **Cloud Abstraction Layer (ADR-004):** 100% (Multi-cloud AWS/Azure support, 5 service abstractions, 46 tests)
- **Titan Neural Memory (ADR-024):** 100% (237 tests, 5 phases deployed, cognitive architecture operational)
- **Real-Time Agent Intervention (ADR-042):** Phase 1 Deployed (CloudTrail, IAM Security Alerting, Checkpoint infrastructure)
- **Repository Onboarding (ADR-043):** 100% (5-step wizard, OAuth GitHub/GitLab, 3 DynamoDB tables, ~8K lines)
- **Customer Onboarding (ADR-047):** 100% (Welcome Modal, Checklist, Tour, Tooltips, Videos, Team Invitations)
- **Developer Tools (ADR-048):** 100% (VSCode, PyCharm, JupyterLab, Dataiku, Fivetran connectors, ~7.7K lines)
- **Self-Hosted Deployment (ADR-049):** Deployed (Podman, hybrid LLM strategy, Windows/Linux/macOS, all 5 phases complete)
- **Self-Play SWE-RL (ADR-050):** 100% Deployed (Bug artifact infra, dual-role self-play, training pipeline, ~15.9K lines, 354 tests)
- **Recursive Context & Embedding (ADR-051):** 100% Complete (RLM 100x context scaling + VL-JEPA 2.85x efficiency, 167 tests)
- **GPU Workload Scheduler (ADR-061):** 100% Deployed (Self-service GPU jobs, queue management, cost controls, 391 tests)
- **Environment Validator Agent (ADR-062):** 100% Deployed (Cross-env validation, drift detection, HITL remediation, 165 tests)
- **Constitutional AI Integration (ADR-063):** 100% Deployed (16 principles, critique-revision pipeline, 463 tests, DEV/QA infrastructure)
- **Customizable Dashboard Widgets (ADR-064):** 100% Deployed (25 widgets, 5 categories, drag-drop editor, role defaults, sharing, ~16.3K lines, 83 tests; 7 ADR-083 runtime security widgets added)
- **Semantic Guardrails Engine (ADR-065):** 100% Deployed (6-layer threat detection, embedding similarity, LLM-as-judge, ~12K lines, 793 tests)
- **Agent Capability Governance (ADR-066):** 100% Deployed (4-tier tool classification, runtime enforcement, dynamic grants, ~5.4K lines, 322 tests)
- **Context Provenance & Integrity (ADR-067):** 100% Deployed (Index-time provenance, trust scoring, anomaly detection, quarantine, ~3.4K lines, 275 tests)
- **Universal Explainability Framework (ADR-068):** 100% Deployed (Reasoning chains, alternatives disclosure, confidence calibration, ~8.2K lines, 227 tests)
- **Guardrail Configuration UI (ADR-069):** 100% Deployed (Compliance profiles, config service, validation, ~1.5K lines, 128 tests)
- **Policy-as-Code GitOps (ADR-070):** 100% Deployed (PolicyValidator, PolicySimulator, OPA Rego validation, ~1.2K lines, 98 tests)
- **Cross-Agent Capability Graph (ADR-071):** 100% Deployed (Neptune graph analysis, escalation paths, coverage gaps, filter UI complete, ~2.4K lines, 179 tests)
- **ML-Based Anomaly Detection (ADR-072):** 100% Deployed (Statistical detector, honeypot capabilities, anomaly explainer, ~1.4K lines, 113 tests)
- **Attribute-Based Access Control (ADR-073):** 100% Deployed (ABAC authorization, clearance levels, multi-tenant isolation, ~1.3K lines, 115 tests)
- **Palantir AIP Integration (ADR-074):** 100% Deployed (Enterprise adapter abstraction, Ontology Bridge sync, event publisher, circuit breaker, ~2.5K lines, 197 tests)
- **Palantir AIP UI Enhancements (ADR-075):** 100% Deployed (8 dashboard widgets, usePalantirSync hook, 5-step config wizard, IntegrationHub wiring complete, ~8K lines, 103 tests)
- **SBOM Attestation & Supply Chain (ADR-076):** 100% Deployed (CycloneDX/SPDX generation, Sigstore signing, dependency confusion, license compliance, ~4.5K lines, 168 tests)
- **Cloud Runtime Security (ADR-077):** 100% Deployed (K8s admission control, container escape detection, runtime-to-code correlation, ~3.2K lines, 155 tests)
- **Air-Gapped & Edge Deployment (ADR-078):** 100% Deployed (Offline model bundles, egress validation, firmware analysis, edge runtime, ~4K lines, 200 tests)
- **Scale & AI Model Security (ADR-079):** 100% Deployed (Streaming analysis, model weight protection, training data poisoning detection, ~2.8K lines, 86 tests)
- **Evo-Memory Enhancements (ADR-080):** 100% Deployed (ReMem Action Framework, 7 operations: CONSOLIDATE, PRUNE, REINFORCE, ABSTRACT, LINK, CORRECT, ROLLBACK, multi-agent sharing, ~10K lines, 336 tests)
- **Constraint Geometry Engine (ADR-081):** Phase 1 Deployed (Deterministic cortical discrimination, 7-axis constraint space, coherence scoring, provenance-aware weighting, 4 policy profiles, ~4.5K lines, 358 tests)
- **Runtime Agent Security Platform (ADR-083):** 100% Deployed (Traffic interception, shadow agent detection, behavioral baselines, AURA-ATT&CK red team (75 techniques), runtime-to-code correlation via GraphRAG, E2E pipeline integrated, 7 dashboard widgets, ~10.3K lines, 848 tests)
- **Native Vulnerability Scanning Engine (ADR-084):** Infrastructure + UI Deployed (GraphRAG-enhanced LLM analysis, 3-phase adversarial verification, 4-layer isolation, closed-loop remediation, Titan Memory integration, ~4.9K lines foundation code, ~3K lines infrastructure, 9 stacks deployed to dev, 20 dashboard widgets, 3 pages, ~9.3K lines UI/mock data, 980 tests planned)
- **Deterministic Verification Envelope (ADR-085):** Proposed (DO-178C output verification architecture, 3 pillars: N-of-M consensus generation, MC/DC structural coverage gate, Z3 formal verification; DO-330 Section 11.4 certification argument; 2 new CGE policy profiles for DAL A/B; ~4.7K lines planned, ~750 tests planned)
- **DEV Cost Scheduler:** Operational (`deploy/cloudformation/dev-cost-scheduler.yaml`, Layer 5.11). Multi-nodegroup off-hours scaling for EKS nodegroups. See runbook for manual override.
- **QA Environment Kill-Switch:** Operational (`scripts/qa_killswitch.py`, 47 tests). See `docs/runbooks/QA_KILLSWITCH_RUNBOOK.md` for operations.
- **DEV Environment Kill-Switch:** Operational (`scripts/dev_killswitch.py`, 86 tests). See `docs/runbooks/DEV_KILLSWITCH_RUNBOOK.md` for operations.
- **Compliance Posture:** NIST 800-53 aligned technical controls (FedRAMP/CMMC certifications not yet pursued)

---

## Development Workflow

### Common Development Tasks

**Testing:**

```bash
pytest tests/                          # Run all tests
pytest tests/test_context_objects.py   # Run specific test
pytest -v                              # Verbose output
```

**Code Quality:**

```bash
black src/                             # Format code
flake8 src/                            # Linting
mypy src/                              # Type checking
```

**Container Builds (Podman-First):** Per ADR-049, Podman is the primary container runtime for local development to avoid Docker Desktop licensing fees.

```bash
# Local development (Podman-first)
podman build --platform linux/amd64 -t my-image .

# Docker available as CI/CD fallback (CodeBuild uses Docker)
docker build --platform linux/amd64 -t my-image .
```

Always use `--platform linux/amd64` on ARM machines (Apple Silicon). See `docs/deployment/DEPLOYMENT_GUIDE.md` for full examples.

**Git Workflow:**

- **Main Branch:** `main` (production-ready code)
- **Development Branch:** `develop` (active development)
- **Feature Branches:** `feature/feature-name`
- **Commit Convention:** Conventional Commits (feat, fix, docs, chore, perf, test, refactor)

**Deployment (Future):**

```bash
# Deploy to dev environment
./deploy/scripts/deploy-network-services.sh dev 2

# Deploy to production
./deploy/scripts/deploy-network-services.sh prod 4
```

---

## Security & Code Review

### **Security Compliance Status (Dec 16, 2025)**

**Infrastructure Security:** 100% GovCloud-ready (19/19 deployed services compatible)
**Security Services:** 100% Deployed (5 Python services, 328 tests)
**Compliance Posture:** NIST 800-53 aligned technical controls deployed.

**Security Services & Infrastructure:** See `docs/security/SECURITY_SERVICES_OVERVIEW.md` for deployed services (5 Python services, 328 tests), CloudFormation resources, and infrastructure controls.

**Organizational Controls:** See internal documentation for status of organizational security controls (AT, IR, PS, RA, CA domains).

**Key Security Requirements:**

1. **IAM Policies - Least Privilege with Documented Exceptions**
   - All `Resource` fields must be scoped to specific resources
   - Use `${ProjectName}-*` naming patterns for resources
   - Add conditions to limit scope (region, tags, service)
   - Example: `Resource: !Sub 'arn:aws:s3:::${ProjectName}-*-${Environment}'`
   - **AWS-Required Wildcards:** Some AWS APIs require `Resource: '*'` (e.g., `ecr:GetAuthorizationToken`, `cloudwatch:PutMetricData`). See ADR-041 for defense-in-depth compensating controls.

2. **Encryption - KMS Customer-Managed Keys**
   - All databases must use customer-managed KMS keys (not AWS-managed)
   - Enable automatic key rotation (`EnableKeyRotation: true`)
   - Scope key policies to specific services via `kms:ViaService` condition
   - Example: See `deploy/cloudformation/neptune.yaml:37-88`

3. **Logging - Extended Retention**
   - VPC Flow Logs: 365 days (prod), 90 days (dev/qa) minimum
   - CloudWatch Logs: 90 days minimum for application logs
   - WAF Logs: 90 days minimum
   - All logs must use KMS encryption where supported

4. **GovCloud Compatibility - ARN Partitions**
   - Never hardcode `arn:aws` - always use `${AWS::Partition}`
   - Use PartitionMap for managed policy ARNs
   - Example: `!Sub 'arn:${AWS::Partition}:service:...'`
   - See `deploy/cloudformation/iam.yaml:4-9` for implementation

5. **IAM Policy Size Limit - 10KB Maximum**
   - AWS enforces a **hard 10,240 byte limit** on inline IAM policies
   - **CRITICAL: Check policy size BEFORE adding new permissions:**
     - Read the existing template and count approximate bytes in the inline policy
     - If adding permissions to a large policy (>300 lines), proactively create a managed policy
     - Never add permissions to inline policies that are already close to the limit
   - **Managed policy pattern (REQUIRED when inline policy is large):**
     ```yaml
     NewPermissionsManagedPolicy:
       Type: AWS::IAM::ManagedPolicy
       Properties:
         ManagedPolicyName: !Sub '${ProjectName}-descriptive-name-${Environment}'
         PolicyDocument:
           Version: '2012-10-17'
           Statement:
             - Effect: Allow
               Action: [action:One, action:Two]
               Resource: [!Sub 'arn:...']
     # Then add !Ref NewPermissionsManagedPolicy to role's ManagedPolicyArns
     ```
   - **Optimization techniques** to stay under limit:
     - Use inline array syntax: `Action: [s3:GetObject, s3:PutObject]` not multi-line
     - Combine resources with wildcards: `${ProjectName}-*-${Environment}` not individual ARNs
     - Use single CloudFormation wildcard: `stack/${ProjectName}-*-${Environment}/*`
     - Avoid duplicate ARN patterns across statements
   - **Validation:** Check policy size BEFORE deploying with `aws iam simulate-custom-policy --policy-input-list` or observe UPDATE_ROLLBACK for "Maximum policy size of 10240 bytes exceeded"
   - **When limit is hit:** Do NOT disable features as a workaround - split into managed policies attached to the role
   - See `deploy/cloudformation/codebuild-observability.yaml` for managed policy pattern example

6. **CRITICAL: Never Commit Secrets to Repository**
   - **NEVER hardcode** passwords, API keys, tokens, or credentials in any file
   - **NEVER commit** `.env` files, private keys (`.pem`, `.key`), or credential files
   - **Always use** SSM Parameter Store (SecureString) or Secrets Manager for sensitive values
   - **Scripts must fetch** credentials at runtime, not embed them
   - **Before committing**, verify no secrets with: `git diff --staged | grep -iE 'password|secret|key|token|credential'`
   - **If secrets were committed:** Rotate them immediately (they're in git history forever)
   - **Allowed patterns:** Example/placeholder values like `AKIAIOSFODNN7EXAMPLE`, `your-api-key-here`, `${SSM_PARAM}`
   - See `scripts/rotate-dev-passwords.sh` for proper credential handling pattern

7. **CRITICAL: Never Hardcode Environment-Specific Values in Templates**
   - **NEVER hardcode** AWS account IDs, endpoints, cluster names, or other environment-specific values in CloudFormation templates
   - **Always use** CloudFormation intrinsic functions: `${AWS::AccountId}`, `${AWS::Region}`, `!Sub`, `!Ref`
   - **For cross-environment data:** Store defaults in Python code, not in CloudFormation
   - **CloudFormation should only reference** the current deployment environment via parameters and intrinsics
   - **Rationale:** Hardcoded values create security risks, break multi-environment deployments, and violate infrastructure-as-code principles

### Security Review

For security-sensitive code changes, reference the security agent template:

- **Agent Template:** `agent-config/agents/security-code-reviewer.md`
- **Coverage:** OWASP Top 10, AI-specific threats, SOX/NIST compliance
- **When to use:** After implementing authentication, authorization, input handling, agent implementations, sandbox management
- **Audit Reports:** See `GOVCLOUD_REMEDIATION_COMPLETE.md` for comprehensive security audit

### Pattern Compliance

**CRITICAL: Check existing patterns in the codebase before proposing new approaches.**

Before implementing any new pattern, utility, or architectural approach:

1. **Search for existing implementations** - Use `grep` or `glob` to find similar code
2. **Review established patterns** - Check how existing buildspecs, templates, or services handle the same task
3. **Follow the established pattern** - Match the existing approach exactly unless there's documented justification
4. **Document deviations** - If a new pattern is truly needed, create an ADR explaining why

**Example:** CloudFormation deployments use `aws cloudformation deploy --no-fail-on-empty-changeset` (see `buildspec-data.yml`). Do not invent create/update branching logic.

---

### Code Quality Standards

- **Clean Code:** Descriptive naming, single responsibility, DRY principle
- **Error Handling:** Proper try-catch, input validation, edge case coverage
- **Type Safety:** Use type hints (Python), avoid `any` (TypeScript)
- **Documentation:** Docstrings for all public functions/classes
- **Testing:** Unit tests for all business logic, integration tests for workflows
- **CloudFormation Validation:** Run `cfn-lint` via wrapper script (see cfn-lint Standards below)
- **CloudFormation Descriptions:** Use layer-based format (see below)
- **No Rounding:** Always use exact percentages and metrics (e.g., 94.7% not 95%, 18/19 not ~95%)
- **AWS CLI Simplicity:** Keep AWS CLI commands concise and focused. Avoid chaining multiple operations with `&&` in complex pipelines, excessive `--query` JMESPath expressions, or inline conditionals. Use simple commands that return clear output. When multiple steps are needed, run them as separate commands.

### CRITICAL: Test Coverage Threshold

**The minimum test coverage threshold of 70% in `pyproject.toml` MUST NOT be lowered under any circumstances.**

- The `fail_under = 70` setting in `[tool.coverage.report]` is a hard requirement
- If coverage drops below 70%, add more tests to increase coverage - never lower the threshold
- This applies to all environments (dev, CI, production)
- Violations of this standard require explicit written approval from the project owner

### CloudFormation Description Standards

**IMPORTANT: Stack Descriptions vs Resource Descriptions**

These standards apply ONLY to the **CloudFormation stack description** (the `Description:` field at the TOP of each template file). They do NOT apply to resource property descriptions inside templates (e.g., the `Description` property of an `AWS::CodeBuild::Project` resource).

- **Stack Description** (line 2 of template): Use the layer-based pattern below
- **Resource Description** (inside AWS resources): Use functional descriptions that explain what the resource does

**Stack Description Patterns:**

CodeBuild Templates:
```
'Project Aura - {LayerName} Layer CodeBuild Project (Layer N.S: Major Services)'
```

Infrastructure Templates:
```
'Project Aura - Layer N.S - ServiceName (Brief Purpose)'
```

Where `N` = layer number (1-8) and `S` = sub-layer number within that layer.

**Sub-Layer Reference:** See `archive/documentation-audits/CLAUDE_MD_ARCHIVED_SECTIONS_2025-12-13.md` or grep `deploy/cloudformation/*.yaml` Description fields.

**Layer Name Reference:**

| Layer | Name | Major Services |
|-------|------|----------------|
| 1 | Foundation | VPC, IAM, WAF, VPC Endpoints, Network Services |
| 2 | Data | Neptune, OpenSearch, DynamoDB, S3 |
| 3 | Compute | EKS, Node Groups, ECR, IRSA |
| 4 | Application | API Services, Bedrock Integration, Frontend |
| 5 | Observability | Secrets, Monitoring, Cost Alerts, Disaster Recovery |
| 6 | Serverless | Lambda, Step Functions, EventBridge, Chat, Runbook, Incident Response |
| 7 | Sandbox | Ephemeral Test Environments, HITL Workflow |
| 8 | Security | AWS Config, GuardDuty, Drift Detection, Red Team |

**Rules:**
- Single-line descriptions only (no multi-line `|` blocks)
- No developer notes, cost analysis, or implementation details
- Brief purpose in parentheses (2-4 words)
- Each infrastructure template has a unique sub-layer number
- Sub-layer numbers reflect deployment order within a layer (e.g., 6.1 → 6.2 → ... → 6.10)
- Integer portion determines layer name (6.1 through 6.10 all use "Serverless" layer name)

### cfn-lint Standards

CloudFormation templates are validated using cfn-lint with standardized exit code handling.

**Configuration:**
- `.cfnlintrc` - Global configuration (ignores W3002, W1020)
- `scripts/cfn-lint-wrapper.sh` - Wrapper script for consistent exit code handling
- `scripts/validate_iam_actions.py` - IAM action validator for W3037 warnings

**Exit Code Handling:**
| Exit Code | Meaning | Action |
|-----------|---------|--------|
| 0 | No errors/warnings | Pass |
| 4 | Warnings only | Non-blocking (pass) |
| 2, 6, 8 | Errors found | Fail build |

**Usage:**
```bash
# Use wrapper script (recommended)
./scripts/cfn-lint-wrapper.sh deploy/cloudformation/template.yaml

# Validate IAM actions (W3037 warnings)
python scripts/validate_iam_actions.py --report

# In buildspecs, always use graceful fallback pattern:
cfn-lint template.yaml || echo "cfn-lint warnings (non-blocking)"
```

**Known Valid IAM Actions:**
When cfn-lint reports W3037 for actions not in its database, add verified actions to `scripts/validate_iam_actions.py` KNOWN_VALID_ACTIONS set after confirming they exist in AWS documentation.

**Nightly Validation:**
`.github/workflows/nightly-iam-validation.yml` runs daily at 2 AM UTC to validate all templates and IAM actions. Creates GitHub issues for invalid actions.

---

## Testing Strategy

> **Full Guide:** `docs/reference/TESTING_STRATEGY.md` - Testing pyramid, mock rationale, patterns

**Quick:** `pytest` (all), `pytest -n auto` (parallel), `pytest -m integration` (integration only)

---

## CI/CD Best Practices

> **Full Guide:** `docs/deployment/CICD_SETUP_GUIDE.md` (563 lines)

**Critical Rules:**
1. **Single Source of Truth:** CodeBuild is the ONLY authoritative deployment method
2. **No Duplicate Builds:** Never trigger CodeBuild while another build is running
3. **No Manual Deployments:** Manual deploys break audit trail and IAM consistency
4. **Buildspec Size Limit:** Max 600 lines per buildspec (`wc -l deploy/buildspecs/*.yml` to check)

**Quick Commands:**
```bash
# Trigger deployment
aws codebuild start-build --project-name aura-compute-deploy-dev

# Check for running builds first
aws codebuild list-builds-for-project --project-name {project} --max-items 1
```

**If you manually deployed:** Delete stack → Redeploy via CodeBuild to restore single source of truth.

---

## Deployment & Infrastructure

- **Infrastructure Status:** See `docs/PROJECT_STATUS.md` for deployed resources and phases
- **dnsmasq Network Services:** See `docs/integrations/DNSMASQ_INTEGRATION.md` for 3-tier DNS architecture
- **Monitoring & Observability:** CloudWatch, Container Insights, Prometheus, SNS alerts deployed

---

## Documentation Maintenance Standard

### CRITICAL: Always Update "The Big 3" After Major Work

After completing any significant feature, bug fix, or infrastructure change, **ALWAYS** update these 3 documents:

| Priority | Document | Update When |
|----------|----------|-------------|
| 1 | `docs/PROJECT_STATUS.md` | Every work session with notable changes |
| 2 | `docs/deployment/DEPLOYMENT_GUIDE.md` | Deployment process changes |
| 3 | `docs/DOCUMENTATION_INDEX.md` | Files added, moved, or archived |

**Quick Checklist:**
- [ ] Updated assessment date and completion percentages in docs/PROJECT_STATUS.md
- [ ] Updated deployment commands (if applicable)
- [ ] Added new files to docs/DOCUMENTATION_INDEX.md (if applicable)

**Note:** `CHANGELOG.md` is auto-generated by Release Please from conventional commit messages. Do NOT manually edit it. Use proper commit prefixes (`feat:`, `fix:`, `docs:`, etc.) - see `docs/deployment/RELEASE_PLEASE_GUIDE.md`.

**What NOT to Update:** `archive/` directory (immutable), stable reference docs (only on major changes)

**Lines of Code:** `cloc src/ deploy/ tests/ --md`

---

## Roadmap

> **Current & Future:** See `docs/PROJECT_STATUS.md` for milestones (Q4 2025 complete, Q1-Q4 2026 planned)

---

## Questions or Issues?

- **Project Documentation:** See `docs/` directory for detailed guides
- **Design Workflows:** See `agent-config/design-workflows/` for UI/UX guidelines
- **Agent Templates:** See `agent-config/agents/` for specialized review agents
- **Research Findings:** See `docs/research/RESEARCH_FINDINGS_CI_CD_DESIGN_WORKFLOWS.md` for automation patterns

**Remember:** Reference documentation files by path instead of reading them in full. This CLAUDE.md contains the most frequently-needed essentials for efficient context management.
