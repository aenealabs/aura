{{- if and (include "aura.isSelfHosted" .) .Values.llm.vllm.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aura.fullname" . }}-vllm
  labels:
    {{- include "aura.componentLabels" (dict "component" "vllm" "context" .) | nindent 4 }}
spec:
  replicas: {{ .Values.llm.vllm.replicaCount }}
  selector:
    matchLabels:
      {{- include "aura.componentSelectorLabels" (dict "component" "vllm" "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "aura.componentSelectorLabels" (dict "component" "vllm" "context" .) | nindent 8 }}
    spec:
      {{- include "aura.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "aura.serviceAccountName" . }}
      securityContext:
        fsGroup: 1000
      containers:
        - name: vllm
          image: {{ .Values.llm.vllm.image.repository }}:{{ .Values.llm.vllm.image.tag }}
          imagePullPolicy: {{ include "aura.imagePullPolicy" . }}
          args:
            - --model
            - {{ .Values.llm.vllm.model }}
            - --host
            - "0.0.0.0"
            - --port
            - "8000"
            - --served-model-name
            - {{ .Values.llm.vllm.model | replace "/" "-" }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: HF_HOME
              value: /models
            - name: VLLM_USAGE_STATS
              value: "false"
            {{- if include "aura.isAirGapped" . }}
            - name: HF_HUB_OFFLINE
              value: "1"
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.llm.vllm.resources | nindent 12 }}
          volumeMounts:
            - name: models
              mountPath: /models
            - name: shm
              mountPath: /dev/shm
      volumes:
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 8Gi
      {{- with .Values.llm.vllm.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.llm.vllm.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if .Values.llm.vllm.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: models
      spec:
        accessModes:
          - ReadWriteOnce
        {{- if .Values.llm.vllm.persistence.storageClass }}
        storageClassName: {{ .Values.llm.vllm.persistence.storageClass }}
        {{- else if .Values.global.storageClass }}
        storageClassName: {{ .Values.global.storageClass }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.llm.vllm.persistence.size }}
  {{- end }}
{{- end }}
