# Project Aura - Agent Dependencies
#
# This file contains dependencies for Coder, Reviewer, and Validator agents.
# Includes tree-sitter for AST parsing, excludes torch (neural memory only).
#
# Usage:
#   pip install -r requirements-agents.txt

# Include all API dependencies
-r requirements-api.txt

# AST Parsing (tree-sitter)
# Cap must match requirements.txt. 0.26.0 removed parser.timeout_micros (the DoS
# guard at src/services/vulnerability_scanner/parsing/ast.py:205) with no usable
# replacement -- progress_callback is ignored for bytestrings and segfaults with a
# reader source. This is the image that actually runs the parser, so the cap
# matters most here. Deferred: docs/DEFERRED_WORK_REGISTRY.md (Upstream-maintainer-gate).
tree-sitter>=0.25.2,<0.26
tree-sitter-python>=0.25.0
tree-sitter-javascript>=0.25.0
tree-sitter-typescript>=0.23.2
tree-sitter-go>=0.23.4
tree-sitter-java>=0.23.5
tree-sitter-rust>=0.24.2
tree-sitter-c>=0.23.4
tree-sitter-cpp>=0.23.4
