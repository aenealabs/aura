AWSTemplateFormatVersion: '2010-09-09'
Description: 'Project Aura - Layer 4.2 - ECR API (Application Registry)'

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - qa
      - prod
    Description: Environment name

  ProjectName:
    Type: String
    Default: aura
    Description: Project name

Resources:
  ApiRepository:
    Type: AWS::ECR::Repository
    DeletionPolicy: Retain
    Properties:
      RepositoryName: !Sub '${ProjectName}-api-${Environment}'
      ImageScanningConfiguration:
        ScanOnPush: true
      # MUTABLE allows :latest tag updates (common for dev/qa)
      # Consider IMMUTABLE for production with semantic versioning
      ImageTagMutability: MUTABLE
      EncryptionConfiguration:
        EncryptionType: AES256
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Keep last 20 tagged images",
                "selection": {
                  "tagStatus": "tagged",
                  "tagPrefixList": ["v", "latest", "dev", "qa", "prod"],
                  "countType": "imageCountMoreThan",
                  "countNumber": 20
                },
                "action": {
                  "type": "expire"
                }
              },
              {
                "rulePriority": 2,
                "description": "Delete untagged images older than 14 days",
                "selection": {
                  "tagStatus": "untagged",
                  "countType": "sinceImagePushed",
                  "countUnit": "days",
                  "countNumber": 14
                },
                "action": {
                  "type": "expire"
                }
              }
            ]
          }
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Environment
          Value: !Ref Environment
        - Key: Component
          Value: api
        - Key: Layer
          Value: '4'

Outputs:
  RepositoryUri:
    Description: ECR Repository URI for API image
    Value: !GetAtt ApiRepository.RepositoryUri
    Export:
      Name: !Sub '${ProjectName}-ecr-api-${Environment}-RepositoryUri'

  RepositoryArn:
    Description: ECR Repository ARN
    Value: !GetAtt ApiRepository.Arn
    Export:
      Name: !Sub '${ProjectName}-ecr-api-${Environment}-RepositoryArn'

  RepositoryName:
    Description: ECR Repository Name
    Value: !Ref ApiRepository
    Export:
      Name: !Sub '${ProjectName}-ecr-api-${Environment}-RepositoryName'
